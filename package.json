{
  "name": "todo-app-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --coverage"
  },
  "devDependencies": {
	"cypress": "^13.0.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/public/**/*.test.js"], 
    "setupFiles": ["./public/jest.setup.js"]
  }
}